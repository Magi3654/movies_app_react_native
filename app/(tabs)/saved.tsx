import { View, Text } from "react-native";

export default function SavedScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-secondary">
      <Text className="text-light-100 text-lg font-bold">Saved Screen!</Text>
    </View>
  );
}