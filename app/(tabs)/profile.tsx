import { View, Text, Image, TouchableOpacity } from "react-native";

export default function ProfileScreen() {
  return (
    <View className="flex-1 bg-primary">
      <View className="flex items-center justify-center bg-dark-100 py-6">
        <Text className="text-light-100 text-xl font-bold mt-4">Profile Screen</Text>
      </View>

      
    </View>
  );
}